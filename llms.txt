# @zakkster/lite-leakforge

> Leak specimens, CI harness, and diagnostic toolkit for @zakkster/lite-leak.

## What this package does

lite-leakforge is the product layer above lite-leak's leak tracker.
It provides three capabilities:

1. SPECIMENS: Deterministic leak scenarios that trigger lite-leak's
   kernels (seven specimens: the raw FR path plus one per patchable
   kernel). Each specimen declares expected events across three
   detection channels (FR leak reports, warnings, findings). The
   verify() contract diffs expected vs actual. Each specimen is the
   kernel's acceptance test and ships in the same session as the kernel.

2. CI HARNESS: assertNoLeaks(fn) and leakSuite() for node:test.
   Exit codes: 0=clean, 1=confirmed leak, 3=inconclusive.
   Precedence: leaks/findings -> 1 even if FR did not settle;
   3 only for evidence-free unsettled runs. Requires --expose-gc.
   settleFinalizers() handles FinalizationRegistry non-determinism
   with bounded retry. Kernel teardown is exception-safe (try/finally
   around user code).

3. CLI (npx leakforge): run a leak-suite file's checks under one
   gate and exit 0/1/3 (evidence-wins). Suite file default-exports
   { name, checks:[{ name, run(tracker) }], options? }. --specimens
   verifies the built-in specimens (kernel acceptance). --json writes
   a machine-readable CI artifact. Self-relaunches with --expose-gc.
   Completes the CLI trilogy with litecap and gc-profiler.

4. FORMATTERS + DASHBOARD: ASCII formatters for leak reports,
   findings, warnings, and owner paths. Dashboard data model with
   ghost-safe signal design (exactly 2 signals, zero per-event churn)
   and LAZY entry formatting (text/ownerPath/label are memoized
   getters -- zero formatting cost per event). createDashboard()
   renders the newest maxLogRows as a sliding window behind a
   dirty flag + power-of-2 frame mask.

## Key exports

```
import { assertNoLeaks, leakSuite, createLeakGate } from '@zakkster/lite-leakforge/harness';
import { verify, composeScenario, createTimerOrphanSpecimen } from '@zakkster/lite-leakforge/scenarios';
import { formatReport, formatOwnerPath, summarize } from '@zakkster/lite-leakforge/formatters';
import { createDashboardModel } from '@zakkster/lite-leakforge/panels';
// CLI: npx leakforge <suite-file> [--json out.json] | --specimens [name...]
```

## Specimen names

raw-fr, timer-orphan, listener-orphan, observer-orphan,
detached-dom, async-retention, raf-orphan.

## Dependencies

- @zakkster/lite-leak ^1.1.0 (runtime; raf-orphan kernel)
- @zakkster/lite-signal >=1.5.0-beta.3 <2.0.0 (peer)

## Conventions

- Single-file ESM modules, MIT license, sideEffects: false
- node:test only (never vitest)
- ASCII-only source (no box-drawing chars in output)
- Verify contract runs: inject -> audit -> release -> settle
- verify() THROWS for needsSettle specimens without --expose-gc
  (env error, not a detection FAIL); pre-FR specimens run anywhere
- Subpath exports ship their own .d.ts files

HARDENING (1.4.1)
-----------------
- gate.run() AWAITS the check fn; a clean `await setTimeout` check no longer
  reports as a timer-orphan leak. run() also serialises process-wide (module
  promise chain) so overlapping runs cannot strand a dead wrapper on a global;
  kernel registration unwinds on failure.
- Contract deepEqual: Object.is (NaN), class gate via Object.prototype.toString
  (Date/Map/Set/RegExp/TypedArray compare by identity, not empty key-walk),
  Array.isArray symmetry, own-property check, seen-map + MAX_TAG_DEPTH cycle
  guard. Fixes silent tag false-PASS.
- settleFinalizers clamps pressureKB/maxRounds (safeCount, 256MB/1000 caps) and
  rejects a non-number check() return. createDashboardModel clamps logCapacity.
  verify(null)/malformed specimen throws a named error.
- Exit codes: EXIT_ERROR = 3 (inconclusive). A throwing check is isolated as an
  [ERROR] row (run continues); unwritable --json and import failure -> 3; empty
  --json= -> usage (2). Evidence wins.
- Dashboard dispose() cancels its raf; selection held by entry identity not
  filtered index. Formatters tolerate null/primitive/hostile-getter events.
- torture/ suite: 106 tests, `npm run torture`.

BASELINE (1.5.0)
----------------
--baseline <path>       gate on findings NEW since the baseline only.
--update-baseline       with --baseline, (re)write the baseline from this run.
buildBaseline/compareBaseline/renderBaselineReport exported.
  A cluster (kind:reason) REGRESSES if new, or if its count exceeds the baseline
  count. A shrunk/vanished cluster is an improvement (reported, never fails).
  Keys are kind:reason, ORIGIN-FREE ON PURPOSE: origins are stack strings whose
  line numbers move on any edit; keying on them would break the baseline every
  commit. Coarser but stable.
  FAIL CLOSED: missing/unreadable baseline -> exit 3 (inconclusive) with a hint,
  never a silent pass. Malformed/unsupported baselineFormat throws. Purpose:
  adopt the gate on a codebase that already leaks without a "fix everything
  first" cliff.
